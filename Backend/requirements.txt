fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
# psycopg2-binary is only needed when running against PostgreSQL.
# For local dev without Postgres, leave DATABASE_URL unset and SQLite is used automatically.
psycopg2-binary>=2.9,<3
python-dotenv==1.0.1
pydantic[email]>=2.11,<3
email-validator==2.2.0
